{
  "name": "netstar-auditor-companion",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace @netstar/web",
    "dev:api": "npm run dev --workspace @netstar/api",
    "build": "npm run build --workspace @netstar/shared && npm run build --workspace @netstar/web && npm run build --workspace @netstar/api"
  }
}
