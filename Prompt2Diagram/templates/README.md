This folder is reserved for future mockup-specific assets such as desk textures, scene props, or reusable material presets.

The first version keeps template assembly code in `src/mockups.js`, but new templates can reference assets stored here.
